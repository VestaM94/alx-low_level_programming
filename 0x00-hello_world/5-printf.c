#include <stdio.h>

/**
 * main - hello printf
 *
 * main function  prints "with proper grammar, but the outcome is a piece of
 * art,"
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
